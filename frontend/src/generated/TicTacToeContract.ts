// Generated by: runar codegen
// Source: TicTacToe
// Do not edit manually.

import { RunarContract, buildP2PKHScript } from 'runar-sdk';
import type { Provider, Signer, Transaction, DeployOptions, RunarArtifact, PreparedCall } from 'runar-sdk';

type CallResult = { txid: string; tx: any };

export interface TerminalOutput {
  satoshis: number;
  /** Recipient address (converted to P2PKH script). */
  address?: string;
  /** Raw locking script hex (used if address is not provided). */
  scriptHex?: string;
}

export interface TicTacToeStatefulCallOptions {
  satoshis?: number;
  changeAddress?: string;
  changePubKey?: string;
  newState?: Record<string, unknown>;
  outputs?: Array<{ satoshis: number; state: Record<string, unknown> }>;
}

export class TicTacToeContract {
  private readonly inner: RunarContract;

  constructor(
    artifact: RunarArtifact,
    args: {
      playerX: string | null;
      betAmount: bigint;
    },
  ) {
    this.inner = new RunarContract(artifact, [args.playerX, args.betAmount]);
  }

  connect(provider: Provider, signer: Signer): void {
    this.inner.connect(provider, signer);
  }

  async deploy(options?: DeployOptions): Promise<CallResult>;
  async deploy(provider: Provider, signer: Signer, options?: DeployOptions): Promise<CallResult>;
  async deploy(...args: unknown[]): Promise<CallResult> {
    return (this.inner.deploy as Function)(...args);
  }

  get contract(): RunarContract {
    return this.inner;
  }

  private static resolveOutputs(outputs: TerminalOutput[]): Array<{ scriptHex: string; satoshis: number }> {
    return outputs.map((o) => {
      const scriptHex = o.scriptHex ?? buildP2PKHScript(o.address!);
      return { scriptHex, satoshis: o.satoshis };
    });
  }

  /** State-mutating method: join */
  async join(opponentPK: string | null, options?: TicTacToeStatefulCallOptions): Promise<CallResult> {
    return this.inner.call('join', [opponentPK, null], options);
  }

  async prepareJoin(opponentPK: string | null, options?: TicTacToeStatefulCallOptions): Promise<PreparedCall> {
    return this.inner.prepareCall('join', [opponentPK, null], options);
  }

  async finalizeJoin(prepared: PreparedCall, sig: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 1: sig });
  }

  /** State-mutating method: move */
  async move(
    position: bigint,
    player: string | null,
    options?: TicTacToeStatefulCallOptions,
  ): Promise<CallResult> {
    return this.inner.call('move', [position, player, null], options);
  }

  async prepareMove(
    position: bigint,
    player: string | null,
    options?: TicTacToeStatefulCallOptions,
  ): Promise<PreparedCall> {
    return this.inner.prepareCall('move', [position, player, null], options);
  }

  async finalizeMove(prepared: PreparedCall, sig: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 2: sig });
  }

  /** Terminal method: moveAndWin */
  async moveAndWin(
    position: bigint,
    player: string | null,
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<CallResult> {
    return this.inner.call('moveAndWin', [position, player, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async prepareMoveAndWin(
    position: bigint,
    player: string | null,
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<PreparedCall> {
    return this.inner.prepareCall('moveAndWin', [position, player, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async finalizeMoveAndWin(prepared: PreparedCall, sig: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 2: sig });
  }

  /** Terminal method: moveAndTie */
  async moveAndTie(
    position: bigint,
    player: string | null,
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<CallResult> {
    return this.inner.call('moveAndTie', [position, player, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async prepareMoveAndTie(
    position: bigint,
    player: string | null,
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<PreparedCall> {
    return this.inner.prepareCall('moveAndTie', [position, player, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async finalizeMoveAndTie(prepared: PreparedCall, sig: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 2: sig });
  }

  /** Terminal method: cancelBeforeJoin */
  async cancelBeforeJoin(
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<CallResult> {
    return this.inner.call('cancelBeforeJoin', [null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async prepareCancelBeforeJoin(
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<PreparedCall> {
    return this.inner.prepareCall('cancelBeforeJoin', [null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async finalizeCancelBeforeJoin(prepared: PreparedCall, sig: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 0: sig });
  }

  /** Terminal method: cancel */
  async cancel(
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<CallResult> {
    return this.inner.call('cancel', [null, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async prepareCancel(
    changePKH: string,
    changeAmount: bigint,
    outputs: TerminalOutput[],
  ): Promise<PreparedCall> {
    return this.inner.prepareCall('cancel', [null, null, changePKH, changeAmount], {
      terminalOutputs: TicTacToeContract.resolveOutputs(outputs),
    });
  }

  async finalizeCancel(prepared: PreparedCall, sigX: string, sigO: string): Promise<CallResult> {
    return this.inner.finalizeCall(prepared, { 0: sigX, 1: sigO });
  }
}
